version: "2"
sql:
  - engine: "mysql"
    schema: "db/schema.sql"
    queries: "db/queries/"
    gen:
      go:
        emit_json_tags: true
        package: "repository"
        out: "internal/repository"
